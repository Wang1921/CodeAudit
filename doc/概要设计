# 概要设计文档：双轨制多智能体代码审计系统
**版本**: V5.0  |  **状态**: 正式 (Final)

## 1. 系统概述 (System Overview)
传统的静态应用安全测试 (SAST) 工具在面对复杂的代码库时，常面临高误报率、跨文件调用链断裂以及无法发现业务逻辑漏洞的痛点。

本系统旨在构建一个**高度自动化、具备专家级推理能力**的代码审计引擎。通过集成 `opencode` CLI 环境，结合 **Google A2A (Agent-to-Agent) 协议理念**，利用多个具有特定安全专业领域的大模型智能体 (Agents) 进行协同作战。系统采用独特的**双轨制（自底向上与自顶向下并行）**架构，实现对技术型漏洞（如注入、路径遍历）和业务逻辑漏洞（如越权、条件竞争）的全面覆盖。

## 2. 核心架构理念 (Core Architectural Principles)
### 2.1 双轨并行审查 (Dual-Track Auditing)
+ **技术轨道 (Bottom-Up)**: 从底层危险函数 (Sinks) 向上逆向追踪，专攻 CWE-22（路径遍历）、CWE-73（文件外部控制）及各类注入漏洞。
+ **业务轨道 (Top-Down)**: 从 API 路由入口向下正向推演，专攻 IDOR（越权）、并发等状态机逻辑漏洞。

### 2.2 文件系统即总线 (A2A over File System IPC)
+ 摒弃复杂的内存消息队列，采用本地文件系统目录作为异步消息总线。
+ Agent 之间通过读写强类型的 JSON 信封 (TaskRequest / TaskResult) 进行任务流转和跨节点求助，天然具备极强的可观测性。

### 2.3 基于 OpenCode 沙盒的无头执行 (Headless Execution)
+ 系统不预先硬编码静态分析工具集。
+ 每个 Agent 作为一个独立的 `opencode` CLI 进程拉起 （工作目录挂载为源码根目录）  ，通过大模型自主编写探测脚本、调用原生 LSP (语言服务器协议) 进行动态的代码片段提取与跳转。

### 2.4 红蓝对抗验证 (Adversarial Validation)
+ 摒弃单一的漏洞判定器。引入具备“黑客思维”的红队智能体负责构造 Payload 和绕过思路；引入具备“防御思维”的蓝队智能体负责审查代码库中的过滤器、强类型转换及后置补偿机制（如循环内的阈值熔断），通过博弈消除误报。

### 2.5 控制面与数据面分离 (Control Plane vs Data Plane)
+ **数据面 (Agents)**: 智能体仅作为无状态的计算节点运行。它们被剥夺了写文件的权限，所有分析结果的 JSON 只能通过**标准输出 (Stdout)** 打印。
+ **控制面 (Python 引擎)**: 唯一拥有本地磁盘写权限的全局调度器。负责监听 Agent 的 Stdout，清洗格式后，代笔写入 `.a2a_bus` 文件系统总线。

### 2.6 最小权限原生沙盒 (Least Privilege Native Sandbox)
+ 摒弃 Docker 等重型虚拟化技术。通过 OpenCode 的原生启动参数（如 `--tools` 和 `--read-only`），按角色精准分配工具（如剥夺核心推演节点的 Bash 权限，仅保留 LSP 代码跳转能力），从根源杜绝恶意脚本执行。

### 2.7 语言分层猎手矩阵 (Language-Specific Hunter Matrix)
* **消除知识串味**：放弃庞杂的单体通用猎手。将 `SinkHunter` 在工程目录上严格按照 `prompts/hunters/{language}/{vuln_type}.yaml` 进行物理隔离。
* **极致领域对焦**：Java 注入猎手的 Prompt 脑海中只有 JDBC 和 MyBatis；Python 猎手只关注 SQLAlchemy 和 `os.system`。大模型的上下文窗口得到最大化利用，信噪比达到极致。

## 3. 动态泛化与安全底线保障 (Generalization & Security Baseline)

### 3.1 动态追踪策略注入 (Dynamic Tracing Profiles)
面对 Nginx/Lua 等路由与业务逻辑跨文件隔离的复杂架构，`Coordinator` 在全局测绘时会动态生成一份“特化追踪指南”。引擎将其拼接到 `ReverseTracer` 提示词模板中，赋予其跨上下文的溯源能力。

### 3.2 引擎级强制安全红线 (Engine-Level Mandatory Overrides)
调度引擎在任务裂变阶段强制干预，不完全依赖 AI 的自觉性：
1. **语言级全量装配**：Python 引擎解析到 language_stack: "java" 后，会自动遍历 prompts/hunters/java/ 目录下的所有 .yaml 文件，并将它们全部实例化为 SinkHunter 任务下发
2. **通用红线强制兜底**：不论 `Coordinator` 识别为何种语言，引擎必定向总线塞入语言无关的底层猎手（如 CWE-532 `Privacy_Hunter` 敏感日志猎手，CWE-798 `Secret_Hunter` 硬编码凭证猎手）。
3. **逻辑推演 100% 覆盖**：提取出的所有 API 路由，必须毫无遗漏地实例化为 `LogicAuditor` 任务。

## 4. 智能体角色定义 (Agent Roster)
系统由 1 个协调者与 5 个专家级工作节点组成（验证层拆分为红蓝双角色）：

| 角色名称 | 类型 | 职责说明 | 核心输入 (A2A) | 核心输出 (A2A) |
| :--- | :--- | :--- | :--- | :--- |
| **Coordinator** | 调度器 | 负责扫描项目结构与语言栈。提取 API 路由表，并生成动态追踪策略（Tracing Strategy）| 项目根目录 | `TaskRequest` |
| **SinkHunter (集群)** | 专家集群 | 基于静态 YAML 模板加载，专精某一语言的特定底层库，精准提取高危触点。 | 包含特定 `cwe_profile` 的 `TaskRequest` | 包含底层坐标的 `TaskRequest` |
| ┣ _FileIO_Hunter_ | 实例化节点 | 专攻 CWE-22/73。精通 `java.io.*`, `java.nio.*`, `commons-io`, `MultipartFile` 等所有文件读写/路径拼接 API。 | - | - |
| ┣ _Injection_Hunter_ | 实例化节点 | 专攻注入类 (JNDI, LDAP, OGNL, SpEL)。精通 `InitialContext.lookup`, `parseExpression`, `Ognl.getValue` 等执行点。 | - | - |
| ┣ _DbQuery_Hunter_ | 实例化节点 | 专攻 SQL/NoSQL 注入。精通 JDBC 拼接、MyBatis `${}` 规则、Hibernate HQL 等底层查询触点。 | - | - |
| **ReverseTracer** | 专家节点 | （保持不变）接收具体坐标进行溯源。 | 具体的 Sink 坐标 | `VulnerabilityCandidate` |
| **LogicAuditor** | 专家节点 | （保持不变）正向推演业务逻辑。 | 具体的 API 路由 | `VulnerabilityCandidate` |
| **RedValidator** | 攻击验证节点 | （保持不变）扮演红队构造 Payload。 | `VulnerabilityCandidate` | `ExploitAttempt` |
| **BlueValidator** | 防御验证节点 | （保持不变）扮演蓝队核查防御机制。 | `ExploitAttempt` | `ConfirmedVuln` |


## 4. 通信与协同协议 (A2A Communication Protocol)
系统在项目根目录维护 `.a2a_bus/` 目录群作为交互中枢。所有 Agent 必须遵守严格的 JSON Schema 契约。

### 4.1 目录拓扑
+ `.a2a_bus/pending/`: 待处理任务队列。
+ `.a2a_bus/help_req/`: 高优先级跨节点求助队列。
+ `.a2a_bus/completed/`: 已完成任务与审查结果。
+ `.a2a_bus/failed/`: 契约破坏/执行异常的死信队列。



## 5. 核心工作流 (System Workflow)
1. **阶段一：全局测绘 (Initialization)**
    - `Coordinator` 扫描项目，识别到项目为 `Java/Spring Boot` 栈。
    - `Coordinator` 提取出 50 个 API 路由，裂变出 50 个 `LogicAuditor` 任务。
    - `Coordinator`** 根据 Java 漏洞特征库，并发派发 3 个专职底层扫描任务：分别唤醒 **`FileIO_Hunter`**、**`Injection_Hunter`** 和 **`DbQuery_Hunter`**。**
2. **阶段二：双轨执行 (Execution)**
    - 3 个专职的 `SinkHunter` 各司其职，在源码中“大海捞针”。例如 `FileIO_Hunter` 找出了 15 个文件操作触点。
    - 这些触点立刻触发 `ReverseTracer` 进行多并发的逆向调用链拼接。
    - 同时，`LogicAuditor` 并发执行路由的安全逻辑审查。
3. **阶段三：动态协同 (Orchestration)**
    - Python 事件循环持续轮询 `.a2a_bus`，处理正常的结果回收
4. **阶段四：红蓝对抗验证 (Adversarial Validation)**
    - **4.1 攻击推演 (Red Team)**：所有来自双轨执行层的 `VulnerabilityCandidate` 首先进入 `RedValidator`。红队节点不看防御，只专注“如果我要利用这个点，应该怎么构造恶意输入？” 如果连红队都觉得无法利用（如参数完全不可控），则直接丢弃；若可利用，则生成附带 Payload 思路的 `ExploitAttempt` 提交给蓝队。
    - **4.2 防御核查 (Blue Team)**：`BlueValidator` 拿到红队的攻击方案后，带着极强的目的性去审查代码。它会质问：“现有的 `SecurityFilter` 或 `XssSanitizer` 能否拦截红队的这个 Payload？” 如果能拦截，则标记为误报；如果防御被击穿，则正式盖章确认为真实漏洞。
5. **阶段五：报告输出 (Reporting)**
    - 引擎汇总 `Validator` 输出的有效 JSON 数据，生成标准化、包含完整调用栈的 Markdown/SARIF 安全审计报告。

## 6. 技术选型与依赖 (Technology Stack)
+ **中枢调度层**: Python 3.10+ (使用 `subprocess` 结合 `os.listdir` 实现事件驱动轮询)。
+ **计算执行层**: `opencode` CLI 环境 (提供 LSP 探针、沙盒执行、大模型上下文管理)。
+ **数据契约层**: JSON (严格的数据模式校验，确保 LLM 输出的结构化与工程鲁棒性)。

